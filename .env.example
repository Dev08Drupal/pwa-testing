# Archivo de ejemplo de variables de entorno para Render.com
# Copia este archivo y configura las variables en el dashboard de Render.com

# Database (Render.com genera esto automáticamente)
DATABASE_URL=postgresql://user:password@host:5432/database

# Hash Salt (Render.com puede generarlo automáticamente)
HASH_SALT=your-random-hash-salt-here

# Render Environment
RENDER=true

# Environment mode (production o development)
ENVIRONMENT=production

# Nombre del servicio en Render (para trusted hosts)
RENDER_SERVICE_NAME=your-service-name
